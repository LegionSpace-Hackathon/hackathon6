/**
 * React Polyfill 工具
 * Agent 页面简化版本 - 提供最小化实现
 */

/**
 * 初始化 React Polyfill
 * Agent 页面不需要特殊的 polyfill，保持接口兼容性
 */
export const initReactPolyfill = (): void => {
  // Agent 页面不需要特殊的 React polyfill
  // 保持接口兼容性，但不执行任何操作
};

